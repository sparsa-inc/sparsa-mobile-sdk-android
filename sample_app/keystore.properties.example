#   KEYSTORE             optional path to the keystore (defaults to ./upload-keystore.jks)
#   KEYSTORE_PASSWORD    store password
#   KEYSTORE_ALIAS       key alias
#   KEYSTORE_KEY_PASSWORD  key password, only if it differs from the store password

storePassword=
keyAlias=
keyPassword=
